{
  "routes": [
    {
      "method": "GET",
      "path": "/note",
      "handler": "Note.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/note/:_id",
      "handler": "Note.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/note",
      "handler": "Note.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/note/:_id",
      "handler": "Note.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/note/:_id",
      "handler": "Note.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}